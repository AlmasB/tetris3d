language: cpp
compiler:
  - gcc
  - clang
# Change this to your needs

before_install: 
  - sudo apt-get install yum
  - sudo yum install SDL2* SDL_net* SDL_ttf*




install: sudo apt-get install libglew-dev
before_script: autoreconf -i
script: ./configure && make