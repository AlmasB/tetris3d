language: cpp
compiler:
  - gcc
  - clang
# Change this to your needs

before_install: 
  - sudo apt-get update -qq
  - apt-cache search libsdl2




install: sudo apt-get install libglew-dev libsdl2-dev
before_script: autoreconf -i
script: ./configure && make