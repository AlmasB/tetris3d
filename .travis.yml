language: cpp
compiler:
  - gcc
  - clang
# Change this to your needs

before_install: 
  - sudo apt-get install yum
  - sudo yum install SDL2-devel.x86-64
  - sudo yum install SDL2_image-devel.x86-64
  - sudo yum install SDL_net-devel.x86-64
  - sudo yum install SDL_ttf-devel.i686




install: sudo apt-get install libglew-dev
before_script: autoreconf -i
script: ./configure && make